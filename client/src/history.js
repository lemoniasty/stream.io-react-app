// Manage history object via PlainRouter component.
// This approach will help us to manage programmatic navigation inside of the app.
import { createBrowserHistory } from 'history';

export default createBrowserHistory();